export let w = 1000,
  h = 600;

export let margin = {
  right: 40,
  left: 40,
  top: 200,
  bottom: 40
};

export let width = w - margin.right - margin.left,
  height = h - margin.top - margin.bottom;
